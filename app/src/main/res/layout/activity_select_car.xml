<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.pemesanan.SelectCarActivity"
    tools:ignore="HardcodedText">
    <data>
        <variable
            name="viewmodel"
            type="com.example.sewakendaraan.viewModel.SelectCarViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="10dp">
            <androidx.core.widget.NestedScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvKendaraan"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:listitem="@layout/rv_item_kendaraan_small" />
            </androidx.core.widget.NestedScrollView>
            <Button
                android:id="@+id/cancel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="16dp"
                android:paddingTop="16dp"
                android:text="Cancel"
                android:backgroundTint="@color/Purple_Bamma"
                style="@style/Widget.MaterialComponents.Button"
                android:layout_alignParentBottom="true"
                android:onClick="moveCancel"/>
        </RelativeLayout>
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:indeterminate="true"
            android:background="@drawable/bg_rounded_square"
            android:backgroundTint="@color/Transparant"
            android:indeterminateTint="@color/Purple_Bamma"
            android:visibility="@{viewmodel.progressBarMutableLiveData}"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>